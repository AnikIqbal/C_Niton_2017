#include <stdio.h>

int main(){

    int num1;
    int num2;
    a = 5;
    b = 15;

    sum = num1 + num2; // variable sum hasn't been declared previously so it is undefined

    printf("sum = %d",sum);

    return 0;

}
